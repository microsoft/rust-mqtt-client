[toolchain]
channel = "1.88"    # This is the version that we will use for local dev to ensure we are using the same tools for static analysis and formatting
components = ["clippy", "rustfmt", "llvm-tools-preview", "rust-analyzer"]